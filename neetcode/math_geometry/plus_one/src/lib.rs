//! Plus One — LeetCode 66 (Easy)
//!
//! `digits` is a number, most-significant digit first. Add one and return the
//! new digits (watch the carry, e.g. [9] -> [1, 0]).

#![allow(unused_variables)] // remove once you implement the body

pub fn plus_one(digits: Vec<i32>) -> Vec<i32> {
    todo!("implement plus_one")
}
