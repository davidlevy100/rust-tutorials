//! Counting Bits — LeetCode 338 (Easy)
//!
//! For each `i` in `0..=n`, return the number of set bits in `i`.

#![allow(unused_variables)] // remove once you implement the body

pub fn count_bits(n: i32) -> Vec<i32> {
    todo!("implement count_bits")
}
